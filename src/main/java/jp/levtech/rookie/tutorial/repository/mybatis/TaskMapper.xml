<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.levtech.rookie.tutorial.repository.mybatis.TaskMapper">

    <select id="findByDate" parameterType="String" resultType="jp.levtech.rookie.tutorial.model.Todo">
        SELECT id, date, title, memo, tag_id FROM todos WHERE date = #{date}
    </select>

    <select id="findById" resultType="jp.levtech.rookie.tutorial.model.Todo">
        SELECT id, date, title, memo, tag_id FROM todos WHERE id = #{id}
    </select>

    <select id="search" parameterType="String" resultType="jp.levtech.rookie.tutorial.model.Todo">
        SELECT id, date, title, memo, tag_id FROM todos
        WHERE title LIKE #{keyword}
        OR memo LIKE #{keyword}
    </select>

    <insert id="register">
        INSERT INTO todos (date, title, memo, tag_id)
        VALUES (#{date}, #{title}, #{memo}, #{tagId})
    </insert>

    <update id="update">
        UPDATE todos
        SET date = #{date},
            title = #{title},
            memo = #{memo},
            tag_id = #{tagId}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM todos WHERE id = #{id}
    </delete>

</mapper>