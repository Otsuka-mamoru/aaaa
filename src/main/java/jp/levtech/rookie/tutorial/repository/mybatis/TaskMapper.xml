<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.levtech.rookie.tutorial.repository.mybatis.TaskMapper">

    <resultMap id="todoResultMap" type="jp.levtech.rookie.tutorial.model.Todo">
        <id property="id" column="id"/>
        <result property="date" column="date"/>
        <result property="title" column="title"/>
        <result property="memo" column="memo"/>
        <result property="tagId" column="tag_id"/>
        <result property="notifyHour" column="notify_hour"/>
        <result property="notifyMinute" column="notify_minute"/>
        <result property="reminderId" column="reminder_id"/>
    </resultMap>

    <select id="findByDate" parameterType="String" resultMap="todoResultMap">
        SELECT id, date, title, memo, tag_id, notify_hour, notify_minute, reminder_id
        FROM todos WHERE date = #{date}
    </select>

    <select id="findById" resultMap="todoResultMap">
        SELECT id, date, title, memo, tag_id, notify_hour, notify_minute, reminder_id
        FROM todos WHERE id = #{id}
    </select>

    <select id="search" parameterType="String" resultMap="todoResultMap">
        SELECT id, date, title, memo, tag_id, notify_hour, notify_minute, reminder_id
        FROM todos
        WHERE title LIKE #{keyword}
        OR memo LIKE #{keyword}
    </select>

    <select id="findByTagId" parameterType="int" resultMap="todoResultMap">
        SELECT id, date, title, memo, tag_id, notify_hour, notify_minute, reminder_id
        FROM todos WHERE tag_id = #{tagId}
    </select>

    <select id="findByMonth" parameterType="String" resultMap="todoResultMap">
        SELECT id, date, title, memo, tag_id, notify_hour, notify_minute, reminder_id
        FROM todos
        WHERE date LIKE #{yearMonth} || '%'
    </select>

    <insert id="register">
        INSERT INTO todos (date, title, memo, tag_id, notify_hour, notify_minute, reminder_id)
        VALUES (#{date}, #{title}, #{memo}, #{tagId}, #{notifyHour}, #{notifyMinute}, #{reminderId})
    </insert>

    <update id="update">
        UPDATE todos
        SET date = #{date},
            title = #{title},
            memo = #{memo},
            tag_id = #{tagId},
            notify_hour = #{notifyHour},
            notify_minute = #{notifyMinute},
            reminder_id = #{reminderId}
        WHERE id = #{id}
    </update>
    
    <update id="clearReminderId" parameterType="int">
    UPDATE todos SET reminder_id = null WHERE reminder_id = #{reminderId}
</update>

    <delete id="delete">
        DELETE FROM todos WHERE id = #{id}
    </delete>

</mapper>